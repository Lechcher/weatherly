import 'package:flutter/material.dart';

class HourlyForecastCardWidget extends StatelessWidget {
  const HourlyForecastCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
