import 'package:flutter/material.dart';

class DailyForecastCardWidget extends StatelessWidget {
  const DailyForecastCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
